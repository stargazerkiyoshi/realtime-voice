## 1. 方案与规范

- [x] 1.1 固化 10 类 A 通道核心生存场景清单（名称、目标、边界）。
- [x] 1.2 为每个场景定义通过/失败判定条件（必需条件、禁止条件、超时阈值）。
- [x] 1.3 评审并确认结构化结果字段（运行级、场景级、断言级、性能阈值评估字段）。
- [x] 1.4 定义性能阈值分阶段门禁策略（A 观测 / B nightly 软门禁 / C PR 硬门禁）。
- [x] 1.5 固化初始统一阈值：`speech_end -> first_audio` 的 `p50 <= 900ms`、`p95 <= 1500ms`。

## 2. 回归执行能力

- [x] 2.1 建立统一回归入口（支持全量执行与按场景筛选执行）。
- [x] 2.2 实现场景注册表与通用执行器（统一生命周期、超时、失败收敛）。
- [x] 2.3 实现结构化结果输出（JSON）及汇总状态（pass/fail/skip）。
- [x] 2.4 实现性能指标采集与阈值评估输出（初始仅观测，不作为失败条件）。

## 3. 接入与验证

- [x] 3.1 将回归入口接入 CI（先非阻塞模式，保留升级为阻塞的开关）。
- [x] 3.2 增加最小文档：如何运行全量回归、如何执行单场景、如何读取结果。
- [x] 3.3 执行一次全量回归并归档结果样例，确认 10 类场景均可判定。
- [x] 3.4 在 nightly 启用性能阈值软门禁，并记录稳定观察期数据。
- [x] 3.5 达到稳定标准后，将性能阈值升级为 PR 硬门禁。
